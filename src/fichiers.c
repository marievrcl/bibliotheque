//
// Created by Marie Viricel on 06/11/2025.
//

#include <stdio.h>
#include <string.h>
#include "fichiers.h"

void chargerLivres(Livre *livres, int *nbLivres) {
    printf("[TODO] chargerLivres (ouvrira data/livres.txt)\n");
    if (nbLivres) *nbLivres = 0;
    (void)livres;
}
void sauvegarderLivres(Livre *livres, int nbLivres) {
    printf("[TODO] sauvegarderLivres (%d)\n", nbLivres);
    (void)livres;
}
void chargerUtilisateurs(Utilisateur *users, int *nbUsers) {
    printf("[TODO] chargerUtilisateurs (ouvrira data/utilisateurs.txt)\n");
    if (nbUsers) *nbUsers = 0;
    (void)users;
}
void sauvegarderUtilisateurs(Utilisateur *users, int nbUsers) {
    printf("[TODO] sauvegarderUtilisateurs (%d)\n", nbUsers);
    (void)users;
}
void chargerEmprunts(Emprunt *emprunts, int *nbEmprunts) {
    printf("[TODO] chargerEmprunts (ouvrira data/emprunts.txt)\n");
    if (nbEmprunts) *nbEmprunts = 0;
    (void)emprunts;
}
void sauvegarderEmprunts(Emprunt *emprunts, int nbEmprunts) {
    printf("[TODO] sauvegarderEmprunts (%d)\n", nbEmprunts);
    (void)emprunts;
}